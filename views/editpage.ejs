<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>movie4U</title>
  <!-- Font Awesome CDN -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- CSS -->
  <link rel="stylesheet" href="../assets/css/style.css" />
</head>

<body>

  <div class="edit-page">
    <div class="container-edit text-danger">
      <h2 class="text-danger">Edit Movie</h2>
      
      <form id="movieForm" action="/updateData" method="post" class="needs-validation">
        <div class="mb-3">
          <label for="title" class="form-label fs-5 fw-bolder text-danger">Title:</label>
          <input type="hidden" name="id" value="<%= user.id %>">
          <input type="text" id="title" name="title" class="form-control " value="<%= user.title %>" required />
        </div>
        <div class="mb-3">
          <label for="rating" class="form-label fs-5 fw-bolder text-danger">Rating:</label>
          <input type="text" id="rating" name="rating" class="form-control" value="<%= user.rating %>" required />
        </div>
        <div class="mb-3">
          <label for="quality" class="form-label fs-5 fw-bolder text-danger">Quality:</label>
          <input type="text" id="quality" name="quality" class="form-control " value="<%= user.quality %>" required />
        </div>
        <div class="mb-3">
          <label for="description" class="form-label fs-5 fw-bolder text-danger">Description:</label>
          <textarea id="description" name="description" class="form-control" required><%= user.description %></textarea>
        </div>
        <div class="mb-3">
          <label for="image" class="form-label fw-bolder fs-5 text-danger">Image URL:</label>
          <input type="text" id="image" name="image" class="form-control " value="<%= user.image %>" required />
        </div>
        <div class="text-center">
          <button type="submit" class="btn btn-danger w-50 mx-auto">Update Movie</button>
        </div>
      </form>
    </div>
  </div>

</body>

</html>
